name: Hello Workflow

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello to users
        run: echo "Hello Dosto"

      - name: Say Hello to GitHub User
        run: echo "Hello ${{ github.actor }}"
