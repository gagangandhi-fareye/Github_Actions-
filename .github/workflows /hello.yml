name: Hello Workflow

on: 
  push:
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello to users
        run: echo "Hello Dosto"

      - name: Say Hello to username
        run: echo "Hello $USER"
